import { ResourceLinkDirective } from './resource-link.directive';

describe('ResourceLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ResourceLinkDirective();
    expect(directive).toBeTruthy();
  });
});
